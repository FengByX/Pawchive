# Pawchive v1.5.3

## 🚀 性能优化（11 项页面加载提速）

1. **冷启动提速**：缓存阈值检测移入后台协程，应用启动不再阻塞（PERF-001）
2. **Cloudflare 过盾异步化**：加密初始化改为后台执行，避免启动卡顿（PERF-002）
3. **书签 / 搜索历史 / 屏蔽创作者**：数据存储读写从阻塞式 `runBlocking` 改为异步 `Deferred`，避免主线程阻塞（PERF-003/004/005）
4. **阅读进度**：构造阶段移除阻塞式读取（PERF-006）
5. **认证 API 连接池复用**：减少重复建连开销（PERF-007）
6. **API 内存缓存**：改用 LRU 策略，限制缓存大小防止内存膨胀（PERF-008）
7. **Release 启用 R8 混淆**：APK 体积显著减小，代码安全性提升（PERF-009）
8. **列表局部刷新**：创作者名称更新使用 payload 局部刷新，不再重载整卡图片（PERF-010）
9. **视频播放器懒初始化**：ExoPlayer 首次播放时才创建，进入详情页更快

## 🎨 UI 统一

- 全局加载指示器统一为「白色圆底 + 主题色旋转弧」样式（主页/搜索/收藏/创作者/登录/注册/详情页），视频层使用半透明深色底

## 🐛 其他

- 多语言（中文/English/日本語）全面同步

> 安装包：`Pawchive-v1.5.3.apk`（versionCode 53）
