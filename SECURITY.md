# 安全政策

## 支持的版本

本项目采用"最近 2 个 minor + 最新 patch"的支持窗口。稳定线自对应 minor 发布起维护 **3 个月**。

| 版本线 | 状态 | 安全修复 |
|--------|------|---------|
| 1.7.x（当前稳定线） | ✅ 支持 | ✅ |
| 1.6.x | ✅ 支持（至 1.7 发布后 3 个月） | ✅ |
| ≤ 1.5.x | ❌ 已停止支持（EOL） | ❌ |

说明：
- 仅最新 patch 会获得修复（例如 1.7.x 线只修最新的 1.7.x）。
- 历史版本不回溯修补，请升级到受支持版本。
- 版本号语义：`MAJOR.MINOR.PATCH`，其中 PATCH 为缺陷/安全修复，MINOR 为向后兼容的功能新增。

## 报告漏洞

**请勿通过公开 Issue 披露安全漏洞细节。**

使用 GitHub 的私密漏洞报告（Private vulnerability reporting）：

1. 打开仓库 **Security** 标签页
2. 点击 **Report a vulnerability**
3. 填写复现步骤、影响范围、受影响版本、可能的修复方向（如已知）

> 若该入口不可用（仓库未开启此功能），请先提交一个**不含漏洞细节**的 Issue 说明"需要安全联络渠道"，维护者会建立私密沟通。

## 响应时限

| 阶段 | 承诺时限 |
|------|---------|
| 确认收到报告 | 72 小时内 |
| 初步评估（是否成立、影响级别） | 7 日内 |
| 修复与发布 | 按严重级别排序；高危优先，通常在评估后 30 日内发布修复版本 |

以上为**尽力承诺**，非法律意义上的 SLA。本项目由个人维护，无专职安全团队。

## 范围

**在范围内：**

- 本 Android 客户端应用（本仓库代码）
- 凭据处理：`EncryptedSharedPreferences` 的使用、备份排除规则、日志脱敏
- Cloudflare 过盾流程中的凭据生命周期（`cf_clearance` 缓存与注入）
- 依赖引入的已知漏洞（经由本仓库依赖清单）

**不在范围内：**

- 上游服务端 `pawchive.pw` 及其 API 的漏洞 —— 属第三方运营方，请直接报告给上游
- Cloudflare 自身的实现问题
- 需要物理接触已解锁设备、或已 root 设备前提下的问题
- 社会工程、拒绝服务（对上游的大流量压测）

## 安全设计说明（便于评估者快速定位）

| 机制 | 实现位置 |
|------|---------|
| 凭据加密存储（AES256-SIV/GCM，Keystore 绑定主密钥） | `core/.../store/SessionManager.kt`、`core/.../api/CloudflareManager.kt` |
| 云备份与设备迁移排除会话文件 | `app/src/main/res/xml/data_extraction_rules.xml` |
| 凭据仅注入主域，不注入图片 CDN 子域 | `core/.../api/ClearanceInterceptor.kt` |
| R8 代码缩减 + 资源压缩 | `app/build.gradle.kts`（release） |

## 不承诺赏金

本项目不提供漏洞赏金（Bug Bounty）。报告中会致谢（如报告者希望署名）。

## 披露政策

- 修复版本发布后，可公开讨论细节。
- 在修复发布前，请勿公开技术细节或 PoC。
