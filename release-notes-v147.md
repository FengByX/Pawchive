## 变更

- **应用图标放大填充** ：前景猫印图案由中心放大 140%，减少四周留白，使其更完整地填充图标区域
- **创作者详情页搜索栏风格统一** ：改为与首页搜索栏一致的胶囊造型（28dp 圆角），添加 1dp 细描边，固定高度 48dp，统一内边距与外边距
- **公告/修订区域背景主题同步** ：将硬编码黑色 `#131320` / `#1B1B2B` 替换为主题属性 `colorSurfaceVariant` + `colorOutline`，深浅模式自动适配
- **公告区域可折叠** ：点击公告标题行可展开/折叠公告列表，右侧 chevron 箭头指示当前状态

## 新增资源

- `ic_launcher_foreground_scaled.xml`：缩放后的应用图标前景（140%）
- `ic_chevron_up.xml`：公告折叠展开箭头图标
- 新增三语言字符串 `collapse` / `expand`（折叠/展开）

## 版本信息

- versionCode: 43
- versionName: 1.4.7
